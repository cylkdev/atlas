defmodule Atlas do
  @moduledoc """
  Top-level module for the `atlas` app.

  The public surface lives in submodules (`Atlas.CLI`, `Atlas.Crates`,
  `Atlas.S3`, `Atlas.Config`, `Atlas.Log`). This module is
  intentionally empty.
  """
end
